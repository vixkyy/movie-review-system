<!DOCTYPE html>
<html>
  <head>
    <title>MovieReviewSystem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="m-0 p-0 text-light" style="background: black;">

    <nav class="navbar navbar-expand-lg bg-dark">
      <div class="container-fluid px-4">
        <%= link_to "Movie Review System", movies_path, class: "btn btn-warning fw-bold text-black p-2" %>
        <% if user_signed_in? %>
          <div class="d-flex align-items-center">
            <span class="text-secondary fw-semibold me-3">
              Hi, <span class="text-light"><%= current_user.email %></span>
            </span>
            <%= link_to "Sign Out",destroy_user_session_path,method: :delete,class: "btn btn-md btn-light fw-semibold text-dark px-3" %>
          </div>
        <% end %>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |type,msg|%>
        <%if type == "notice"%>
          <div class="alert alert-success"><%= msg%></div>
        <% elsif type== "alert"%>
          <div class="alert alert-danger"><%=msg%></div>
        <%end%>
      <%end%>
    </div>
    <%= yield %>
  </body>
</html>
